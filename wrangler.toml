# Cloudflare Workers static-assets site. `npx wrangler deploy` runs the build, then uploads ./public.
name = "suppliercataluguevalidation"
compatibility_date = "2025-09-01"

[build]
command = "npm run build"

[assets]
directory = "./public"
